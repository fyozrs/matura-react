import ReactMarkdown from "react-markdown";
import content from "./content/exam-material.md";

function ExamContent(){
    <main>
        <ReactMarkdown>{content}</ReactMarkdown>
    </main>
}

export default ExamContent